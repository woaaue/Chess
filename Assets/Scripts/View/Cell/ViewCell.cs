using UnityEngine;

public sealed class ViewCell : MonoBehaviour
{
    //public void LightUpCells(List<Cell> sortedCells)
    //{
    //    for (int i = 0; i < sortedCells.Count; i++)
    //    {
    //        for (int y = 0; y < transform.childCount; y++)
    //        {
    //            var cell = sortedCells[i];

    //            if (cell.X == transform.GetChild(y).transform.position.x && cell.Y == transform.GetChild(y).transform.position.y)
    //            {
    //                var cellObject = transform.GetChild(y).GetComponent<SpriteRenderer>();
    //                cellObject.color = new Color(0.5f, 0.3f, 0.3f);
    //            }
    //        }
    //    }
    //}
}
